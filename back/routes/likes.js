// const express = require("express");
// const router = express.Router(); // Create a new router instance

// // Middleware
// const auth = require("../middlewares/auth"); // Authentication middleware

// // Controller
// const ctrlLike = require("../controllers/likes"); // Importing the likes controller

// // Route to like/unlike an article
// router.post("/", auth, ctrlLike.like);
// // Requires authentication. This route handles both adding (liking) and removing (unliking) a like

// module.exports = router; // Export the configured router
